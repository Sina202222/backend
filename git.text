echo "# blogapi" >> README.md

git init

git add README.md

git add .



git commit -m "first commit"
git commit -m " add setting  django"

git commit -m " add config/urls"
git commit -m "  Add SECRET_KEY + install package (psycopg2-binary,psycopg2) "
git commit -m "  Add tailwind + navbar + pages   "




git branch -M main
git remote add origin https://github.com/Sina202222/blogapi.git
git push -u origin main


git push -u origin main --force